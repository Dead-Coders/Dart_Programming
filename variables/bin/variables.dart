void main() {

// Arithmetic Operators:
int age = 25;
double pi = 3.14159;


int sum = age + 10; // sum will be 35
double area = pi * 2;  // area will be calculated based on radius value


// Assignment Operators:
int count = 0;
count += 3; // count becomes 3
count *= 2; // count becomes 6


// Comparison Operators:
bool isEven = age % 2 == 0; // Checks if age is even

if (temperature > 30) {
  print("It's hot!");
}


// Logical Operators:
bool hasAccess = isLoggedIn && hasPermission; // Requires both conditions to be true

if (!isValidInput || isEmpty) {
  print("Invalid input!");
}

// Conditional (Ternary) Operator:
String message = age >= 18 ? "You are an adult." : "You are a minor.";

}
